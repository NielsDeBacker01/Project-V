import React from 'react';
import './Spinner.css';

//simple spinner to display when something is loading
const Spinner = () => {
  return(  
    <div className="spinner"></div>
  )
};

export default Spinner;